CONFIG_ZTEST=y
CONFIG_ZTEST_ASSERT_VERBOSE=y
CONFIG_LOG=y
CONFIG_LOG_DEFAULT_LEVEL=2

# Disable most drivers/features to keep test binary small
CONFIG_UART=n
CONFIG_USB_DEVICE_STACK=n
CONFIG_NET=n

# Enable only what's needed for core tests
CONFIG_MINIMAL_LIBC=y
